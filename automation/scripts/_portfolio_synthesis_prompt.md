# 跨公司模型组合综合

所有公司承保结果已经终态并通过严格验证，行情快照也已封存。请解释机器生成的组合结果，不得修改操作、仓位和排除原因。

- 批次：{{RUN_ID}}
- 输出：{{OUTPUT_PATH}}

## 封存行情快照

```json
{{QUOTE_SNAPSHOT_JSON}}
```

## 机器组合结果

```json
{{PORTFOLIO_JSON}}
```

## 组合政策

```json
{{PORTFOLIO_POLICY_JSON}}
```

报告必须逐家公司列出现价、悲观价值、合理价值区间、买入区、承保状态、最终操作、仓位和全部落选理由。不要写入其他路径，不要提交 Git。
